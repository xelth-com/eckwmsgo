# Quick Start Guide

## Быстрый старт за 5 минут

### 1. Проверь структуру проекта

Должно быть так:
```
eckwmsgo/
├── cmd/             ← Go backend
│   └── api/
│       └── main.go
├── internal/        ← Handlers, models, services
├── web/             ← SvelteKit frontend
│   ├── src/         ← Исходники фронтенда
│   └── build/       ← Собранный фронтенд (после npm run build)
├── .env             ← Конфигурация
└── eckwms           ← Скомпилированный бинарник (после go build)
```

### 2. Настрой .env

```bash
cd eckwmsgo
```

Проверь что в `.env` правильные настройки:
```env
# Главный порт приложения
PORT=3210

# Database (Zero-config: оставь PG_PASSWORD пустым для Embedded PostgreSQL)
PG_DATABASE=eckwmsgo_local
PG_USERNAME=postgres
PG_PASSWORD=                   # Пусто = автоматически использует Embedded PostgreSQL
PG_HOST=localhost
PG_PORT=5432
DB_ALTER=true

# Security
JWT_SECRET=68695b04ccf8da689a0c1fd000a05941e958bcf5d1091285400a7db43bb5402a
ENC_KEY=d34dac7ad4264dd83dde2b70f4b1b5c065d03723aa0debd2
```

### 3. Собери фронтенд

```bash
cd web
npm install
npm run build
cd ..
```

Это создаст папку `web/build/` со статическими файлами.

### 4. Запусти сервер

**Вариант A: Готовый бинарник (самый простой)**
```bash
./eckwms
```

**Вариант B: Через go run**
```bash
go run ./cmd/api/main.go
```

**Вариант C: Собрать и запустить**
```bash
go build -o eckwms ./cmd/api
./eckwms
```

Увидишь:
```
🚀 Server starting on port 3210
✅ Frontend directory: web/build
✅ Database: Embedded PostgreSQL (or external if configured)
```

### 5. Протестируй

Открой браузер или curl:

**Health check:**
```bash
curl http://localhost:3210/health
# {"status":"ok","server":"local"}
```

**API Status:**
```bash
curl http://localhost:3210/api/status
# {"status":"running","version":"1.0.0"}
```

**Фронтенд:**
Открой в браузере:
```
http://localhost:3210
```

Должен загрузиться современный SvelteKit фронтенд со сканером QR кодов!

## Troubleshooting

### Проблема: "Failed to connect to database"
**Решение:**
1. Если используешь **Embedded PostgreSQL** (PG_PASSWORD пустой):
   - Проверь что порт 5432 свободен
   - Убедись что есть место на диске (~50MB для загрузки)
   - При первом запуске может занять 1-2 минуты (загружает PostgreSQL)
2. Если используешь **внешний PostgreSQL**:
   - Проверь что PostgreSQL запущен
   - Проверь настройки в `.env` (особенно PG_PASSWORD)
   - База данных создастся автоматически если DB_ALTER=true

### Проблема: Фронтенд не загружается (404)
**Решение:**
1. Проверь что фронтенд собран:
   ```bash
   cd web
   npm run build
   ```
2. Проверь что папка `web/build/` существует и содержит `index.html`
3. В логах сервера должно быть: "✅ Frontend directory: web/build"

### Проблема: Порт занят
**Решение:**
Измени порт в `.env`:
```
PORT=3211
```

### Проблема: npm install не работает
**Решение:**
Убедись что Node.js 18+ установлен:
```bash
node --version  # должно быть v18 или выше
npm --version
```

## Следующие шаги

После того как сервер запустится:

1. **Протестируй функциональность**:
   - Зарегистрируйся/залогинься через фронтенд
   - Протестируй QR scanner (камера должна работать)
   - Создай RMA, warehouse, items через UI
2. **Исследуй API**:
   - Используй Postman или curl для тестирования endpoints
   - JWT токен получается после логина
3. **Device Pairing** (опционально):
   - Сгенерируй QR код для паринга устройства
   - Протестируй WebSocket соединение
4. **Деплой в продакшен**:
   - Собери с BASE_PATH для subdirectory deployment
   - Настрой Nginx/Apache
   - Используй внешний PostgreSQL для production

## Полезные команды

```bash
# Backend
go build -o eckwms ./cmd/api              # Пересобрать бинарник
go test ./...                              # Запустить тесты
go mod tidy                                # Обновить зависимости

# Frontend
cd web
npm run build                              # Собрать фронтенд для production
npm run dev                                # Запустить dev server (порт 5173)
BASE_PATH=/E npm run build                 # Собрать для subdirectory deployment

# Проверка кода
go fmt ./...                               # Форматирование
go vet ./...                               # Статический анализ

# Production build
go build -ldflags="-s -w" -o eckwms ./cmd/api  # Компактный бинарник
```

## Нужна помощь?

- **README.md** - полная документация
- **DEPLOYMENT_SUBDIRECTORY.md** - деплой в подпапке URL
- **internal/handlers/** - примеры реализации endpoints
- **internal/models/** - структура данных
- **web/src/** - исходники фронтенда
