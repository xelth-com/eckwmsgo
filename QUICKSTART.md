# Quick Start Guide

## Быстрый старт за 3 минуты

### 1. Проверь структуру папок

Должно быть так:
```
C:\Users\Dmytro\
├── eckwms\          ← Оригинальный Node.js проект (фронтенд)
│   ├── public\      ← React фронтенд здесь
│   ├── src\
│   └── ...
└── eckwmsgo\        ← Новый Go backend (этот проект)
    ├── cmd\
    ├── internal\
    ├── eckwmsgo.exe ← Скомпилированный бинарник
    └── .env
```

### 2. Настрой .env

```bash
cd C:\Users\Dmytro\eckwmsgo
```

Проверь что в `.env` правильные настройки:
```env
# Главное - настройки БД должны совпадать с Node.js версией
PG_DATABASE=inbody_ai_support
PG_USERNAME=inbody_user
PG_PASSWORD=beliberdabeliberden
PG_HOST=localhost
PG_PORT=5432

# JWT секрет
JWT_SECRET=68695b04ccf8da689a0c1fd000a05941e958bcf5d1091285400a7db43bb5402a

# Go сервер на другом порту чтобы не конфликтовать с Node.js
PORT=3001
```

### 3. Запусти сервер

**Вариант A: Готовый бинарник (самый простой)**
```bash
cd C:\Users\Dmytro\eckwmsgo
./eckwmsgo.exe
```

**Вариант B: Через go run**
```bash
cd C:\Users\Dmytro\eckwmsgo
go run ./cmd/api/main.go
```

Увидишь:
```
🚀 Server starting on port 3001
```

### 4. Протестируй

Открой браузер или curl:

**Health check:**
```bash
curl http://localhost:3001/health
# {"status":"ok","server":"local"}
```

**API Status:**
```bash
curl http://localhost:3001/api/status
# {"status":"running","version":"1.0.0"}
```

**Фронтенд:**
Открой в браузере:
```
http://localhost:3001
```

Должен загрузиться React фронтенд из `../eckwms/public`

### 5. Параллельное тестирование

Можешь запустить оба сервера одновременно:

**Терминал 1 - Node.js (порт 3000):**
```bash
cd C:\Users\Dmytro\eckwms
npm run start:local
```

**Терминал 2 - Go (порт 3001):**
```bash
cd C:\Users\Dmytro\eckwmsgo
./eckwmsgo.exe
```

Теперь у тебя:
- Node.js API: `http://localhost:3000`
- Go API: `http://localhost:3001`
- Оба используют одну БД

## Troubleshooting

### Проблема: "Failed to connect to database"
**Решение:**
1. Проверь что PostgreSQL запущен
2. Проверь настройки в `.env`
3. Попробуй подключиться из Node.js версии - если работает, то настройки правильные

### Проблема: Фронтенд не загружается (404)
**Решение:**
1. Проверь что папка `C:\Users\Dmytro\eckwms\public` существует
2. Если структура папок другая, добавь в `.env`:
   ```
   FRONTEND_DIR=C:/Users/Dmytro/eckwms/public
   ```

### Проблема: Порт занят
**Решение:**
Измени порт в `.env`:
```
PORT=3002
```

### Проблема: "go: command not found"
**Решение:**
Go установлен в `C:\Program Files\Go\bin\go.exe`
Используй полный путь:
```bash
/c/Program\ Files/Go/bin/go run ./cmd/api/main.go
```

## Следующие шаги

После того как сервер запустится:

1. **Протестируй API endpoints** - используй Postman или curl
2. **Проверь работу с БД** - создай RMA, warehouse, items
3. **Сравни с Node.js версией** - убедись что данные те же
4. **Разрабатывай дальше**:
   - Добавь JWT аутентификацию
   - Реализуй middleware
   - Добавь тесты
   - Мигрируй остальные сервисы

## Полезные команды

```bash
# Пересобрать бинарник
go build -o eckwmsgo.exe ./cmd/api

# Добавить зависимость
go get -u package-name

# Обновить зависимости
go mod tidy

# Запустить тесты
go test ./...

# Проверить код
go fmt ./...
go vet ./...
```

## Нужна помощь?

- README.md - полная документация
- internal/handlers/ - посмотри как реализованы endpoints
- internal/models/ - структура данных
