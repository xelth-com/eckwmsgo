# 🚀 Инструкция по Push в GitHub

## ✅ Коммит уже создан!

Код уже закоммичен локально. Осталось только запушить в GitHub.

## 📝 Шаги для Push:

### Вариант 1: Используя скрипт (РЕКОМЕНДУЕТСЯ)

#### Windows:
1. Откройте файл `git_push.bat` в любом текстовом редакторе (Notepad, VSCode, etc.)
2. Найдите строку:
   ```bat
   set GITHUB_TOKEN=REPLACE_WITH_YOUR_GITHUB_TOKEN
   ```
3. Замените `REPLACE_WITH_YOUR_GITHUB_TOKEN` на ваш настоящий GitHub токен
4. Сохраните файл
5. Запустите: `git_push.bat`

#### Linux/Mac:
1. Откройте файл `git_push.sh` в текстовом редакторе
2. Найдите строку:
   ```bash
   GITHUB_TOKEN="REPLACE_WITH_YOUR_GITHUB_TOKEN"
   ```
3. Замените `REPLACE_WITH_YOUR_GITHUB_TOKEN` на ваш настоящий GitHub токен
4. Сохраните файл
5. Запустите: `./git_push.sh`

---

### Вариант 2: Вручную через командную строку

```bash
# Замените YOUR_TOKEN_HERE на ваш настоящий токен
git push https://YOUR_TOKEN_HERE@github.com/dmytrosurovtsev/eckwmsgo.git master
```

---

### Вариант 3: Используя SSH (самый безопасный)

1. Создайте SSH ключ (если ещё нет):
   ```bash
   ssh-keygen -t ed25519 -C "your@email.com"
   ```

2. Скопируйте публичный ключ:
   ```bash
   cat ~/.ssh/id_ed25519.pub
   ```

3. Добавьте ключ на GitHub:
   - Откройте https://github.com/settings/keys
   - Нажмите "New SSH key"
   - Вставьте скопированный ключ

4. Измените remote на SSH:
   ```bash
   git remote set-url origin git@github.com:dmytrosurovtsev/eckwmsgo.git
   ```

5. Запушите:
   ```bash
   git push -u origin master
   ```

---

## 🔑 Где получить GitHub Token:

1. Откройте: https://github.com/settings/tokens
2. Нажмите "Generate new token" → "Generate new token (classic)"
3. Введите название: `eckwmsgo-deployment`
4. Выберите срок действия: 90 дней (или Custom)
5. Выберите scope: **repo** (полный доступ к репозиториям)
6. Нажмите "Generate token"
7. **ВАЖНО:** Скопируйте токен сразу - вы не сможете увидеть его снова!

---

## 📊 Что было закоммичено:

```
feat(database,main): Enable zero-config deployment with embedded PostgreSQL

18 файлов изменено, 1526 строк добавлено:
- Embedded PostgreSQL support
- Auto-migration для всех моделей
- Документация (CLAUDE.md, QUICKSTART.md)
- Примеры конфигурации (.env.example)
```

---

## ⚠️ ВАЖНО: Безопасность токена

1. **НИКОГДА** не коммитьте файлы с токеном в git
2. `git_push.bat` и `git_push.sh` уже в `.gitignore` (будут добавлены автоматически)
3. После использования токена, храните его в безопасном месте
4. Регулярно меняйте токены (раз в 90 дней)

---

## ✅ После успешного Push:

Ваш репозиторий будет доступен по адресу:
https://github.com/dmytrosurovtsev/eckwmsgo

